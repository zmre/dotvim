" search behavior
" make searches use regular regexes with next two lines
"nnoremap / /\v
"vnoremap / /\v
setlocal ignorecase      " ignore case in searches
setlocal smartcase
setlocal incsearch       " do incremental searching
