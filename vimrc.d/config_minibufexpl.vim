" control + the vim direction keys [hjkl] moves you between windows.
let g:miniBufExplMapWindowNavVim = 1
" control + tab & shift + control + tab can alternatively be setup to 
" cycle forwards and backwards through your modifiable buffers 
" in the current window
let g:miniBufExplModSelTarget = 1
" Put the mini buff window at the top
let g:miniBufExplSplitBelow=0
